export enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT
}

export const getReverse = (direction: Direction): Direction => {
    // TODO 0: Return the reverse of the direction received as parameter
}